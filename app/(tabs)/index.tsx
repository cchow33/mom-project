import { Text, View } from "../../components/Themed";
import tw from "tailwind-react-native-classnames";

export default function TabOneScreen() {
  return (
    <View style={tw`bg-red h-full`}>
      <Text>For Mom</Text>
    </View>
  );
}
